/// A single study flashcard with a question on the front and an answer on
/// the back.
class Flashcard {
  const Flashcard({required this.question, required this.answer});

  final String question;
  final String answer;
}
