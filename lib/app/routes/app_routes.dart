/// Keeps route names in one place so navigation paths are not repeated.
abstract final class AppRoutes {
  static const login = '/login';
  static const home = '/home';
  static const otpVerification = '/otp-verification';
}
